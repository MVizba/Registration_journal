{% extends 'base.html.twig' %}

{% block title %}Examination{% endblock %}

{% block body %}
    <h1>Examination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examination.id }}</td>
            </tr>
            <tr>
                <th>Shortcut</th>
                <td>{{ examination.shortcut }}</td>
            </tr>
            <tr>
                <th>ExaminationName</th>
                <td>{{ examination.examinationName }}</td>
            </tr>
            <tr>
                <th>Norms</th>
                <td>{{ examination.norms }}</td>
            </tr>
            <tr>
                <th>Machine</th>
                <td>{{ examination.machine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_examination_index') }}">back to list</a>

    <a href="{{ path('app_examination_edit', {'id': examination.id}) }}">edit</a>

    {{ include('examination/_delete_form.html.twig') }}
{% endblock %}
