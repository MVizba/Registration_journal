{% extends 'base.html.twig' %}

{% block title %}Examination{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h1 class="text-left">Tyrimo informacija</h1>

                <table class="table">
                    <tbody>
                    <tr>
                        <th>Shortcut</th>
                        <td>{{ examination.shortcut }}</td>
                    </tr>
                    <tr>
                        <th>Examination Name</th>
                        <td>{{ examination.examinationName }}</td>
                    </tr>
                    <tr>
                        <th>Norms</th>
                        <td>{{ examination.norms }}</td>
                    </tr>
                    <tr>
                        <th>Machine</th>
                        <td>{{ examination.machine }}</td>
                    </tr>
                    </tbody>
                </table>

                <div class="row justify-content-left mt-3">
                    <div class="col-auto">
                        <button type="button" class="btn btn-secondary" onclick="window.history.back();">Atgal</button>
                    </div>
{#                    <div class="col-auto">#}
{#                        <a href="{{ path('app_examination_edit', {'id': examination.id}) }}" class="btn btn-secondary">Edit</a>#}
{#                    </div>#}
{#                    <div class="col-auto">#}
{#                        {{ include('examination/_delete_form.html.twig') }}#}
{#                    </div>#}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
