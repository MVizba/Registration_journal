{% extends 'base.html.twig' %}

{% block title %}New DrugWarehouse{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Pridėti medikamentą</h1>
        <div class="alert alert-info">
            <i class="fas fa-info-circle"></i> Užpildykite medikamento informaciją.
        </div>
        {{ form_start(form) }}
        <div class="card p-4 shadow">
            {{ include('drug_warehouse/_form.html.twig', {'button_label': 'Išsaugoti'}) }}
        </div>
        {{ form_end(form) }}
        <button type="button" class="btn btn-secondary mt-3" onclick="window.history.back();">Atgal</button>
    </div>
{% endblock %}
