<form method="post" action="{{ path('app_asigned_drugs_delete', {'id': asigned_drug.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asigned_drug.id) }}">
    <button class="btn">Delete</button>
</form>
