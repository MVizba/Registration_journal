<form method="post" action="{{ path('app_examination_w_r_delete', {'id': examination_with_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examination_with_result.id) }}">
    <button class="btn">Delete</button>
</form>
