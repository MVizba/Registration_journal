{% extends 'base.html.twig' %}

{% block title %}Edit ExaminationWithResults{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center">Redaguoti tyrimo rezultatus</h1>

        <div class="row justify-content-center">
            <div class="col-md-8">
                {{ include('examination_wr/_form.html.twig', {'button_label': 'Atnaujinti'}) }}

                <div class="row mt-3 justify-content-left">
                    <div class="col-auto">
                        <a href="{{ path('app_appointment_edit', {'id': examination_with_result.appointment.id}) }}" class="btn btn-secondary">atgal</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
