{% extends 'base.html.twig' %}

{% block title %}ExaminationWithResults{% endblock %}

{% block body %}
    <h1>ExaminationWithResults</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examination_with_result.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ examination_with_result.date ? examination_with_result.date|date('Y-m-d H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ examination_with_result.result }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_examination_w_r_index') }}">back to list</a>

    <a href="{{ path('app_examination_w_r_edit', {'id': examination_with_result.id}) }}">edit</a>

    {{ include('examination_wr/_delete_form.html.twig') }}
{% endblock %}
